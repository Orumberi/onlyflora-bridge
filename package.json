{
  "name": "onlyflora-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "A narrow, authenticated MCP bridge between ChatGPT/Codex and the OnlyFlora Webasyst catalog.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/mcp.js && node --check src/oauth.js && node --check src/webasyst.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "express": "5.1.0",
    "express-rate-limit": "8.1.0",
    "helmet": "8.1.0",
    "jose": "6.1.0",
    "zod": "4.1.5"
  }
}
